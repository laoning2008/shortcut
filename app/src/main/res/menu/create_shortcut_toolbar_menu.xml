<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/toolbar_menu_create_shortcut_cancel"
        android:icon="@drawable/ic_baseline_undo_24"
        android:title="取消"
        app:iconTint="?attr/colorOnSecondary"
        app:showAsAction="always"/>

    <item
        android:id="@+id/toolbar_menu_create_shortcut_done"
        android:icon="@drawable/ic_baseline_done_24"
        android:title="完成"
        app:iconTint="?attr/colorOnSecondary"
        app:showAsAction="always"/>

    <item
        android:id="@+id/toolbar_menu_create_shortcut_add_action"
        android:icon="@drawable/ic_baseline_add_24"
        android:title="添加操作"
        app:iconTint="?attr/colorOnSecondary"
        app:showAsAction="always"/>
    <item
        android:id="@+id/toolbar_menu_create_shortcut_run"
        android:icon="@drawable/baseline_directions_run_24"
        android:title="运行"
        app:iconTint="?attr/colorOnSecondary"
        app:showAsAction="always"/>
</menu>